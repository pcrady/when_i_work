A script to download csv files and reshape them into a new csv
called ouput.csv. Its pretty slow because I'm not very good at
database manipulation. 

to run:
python script.py

Note:
You may need to install pandas. To do so:
$sudo pip install pandas

